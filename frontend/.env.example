# Public backend base that the SPA uses in browser (defaults to same-origin
# /api so the included nginx-proxy container resolves it to the API service).
VITE_API_BASE=/api

# Dev-only: where Vite dev server forwards /api to during `npm run dev`.
# Leave empty to default to http://localhost:5277.
VITE_BACKEND_URL=
